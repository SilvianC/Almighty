server {
  listen 443 ssl;
  server_name k9a201.p.ssafy.io;
  server_tokens off;
  access_log /var/log/nginx/access.log;
  error_log /var/log/nginx/error.log;
  client_max_body_size 200M;

  ssl_certificate /etc/letsencrypt/live/k9a201.p.ssafy.io/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/k9a201.p.ssafy.io/privkey.pem;
  include /etc/letsencrypt/options-ssl-nginx.conf;
  ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

  location /api { 
    proxy_pass k9a201.p.ssafy.io:8080;
    proxy_set_header Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    try_files $uri $uri/ /index.html;
  }
}

server {
  if ($host = k9a201.p.ssafy.io) {
    return 301 https://$host$request_uri;
  } # managed by Certbot
  listen 80;
  server_name k9a201.p.ssafy.io;
    return 404; # managed by Certbot
}